# 🚀 Полная инструкция по деплою на Vercel с большим приложением

## Проблема и Решение

**Проблема:** Vercel имеет лимит ~25 МБ на размер функции, но наш exe файл занимает ~600 МБ.

**Решение:** Использовать **GitHub Releases** для хостинга exe файла (бесплатно, без лимитов), а на Vercel размещать только легкий веб-сайт.

---

## 📋 Подготовка

### 1. Создайте GitHub репозиторий (если еще нет)

```powershell
cd d:\Test\CascadeProjects\2048\avalon-checker-website
git init
git add .
git commit -m "Initial commit: Avalon Checker Website"
```

Создайте новый репозиторий на GitHub: https://github.com/new

```powershell
git remote add origin https://github.com/ВАШ_USERNAME/avalon-checker-website.git
git branch -M main
git push -u origin main
```

---

## 🎯 Шаг 1: Создание GitHub Release с exe файлом

### 1.1. Перейдите на страницу Releases вашего репозитория
- `https://github.com/ВАШ_USERNAME/avalon-checker-website/releases`
- Нажмите **"Create a new release"**

### 1.2. Заполните информацию о релизе
- **Tag version:** `v3.0.0`
- **Release title:** `Avalon Checker v3.0.0`
- **Description:** Опишите изменения в версии

### 1.3. Загрузите exe файл
- Нажмите **"Attach binaries by dropping them here or selecting them"**
- Выберите файл: `d:\Test\CascadeProjects\2048\avalon-checker-website\public\release\AvalonChecker-Setup-3.0.0.exe`
- Дождитесь завершения загрузки (может занять несколько минут для 600 МБ)

### 1.4. Опубликуйте релиз
- Нажмите **"Publish release"**

### 1.5. Скопируйте URL загрузки
После публикации:
- Правой кнопкой на имя файла → **"Copy link address"**
- URL будет вида: `https://github.com/ВАШ_USERNAME/avalon-checker-website/releases/download/v3.0.0/AvalonChecker-Setup-3.0.0.exe`

---

## ⚙️ Шаг 2: Обновите конфигурацию релиза

Откройте файл `src/config/release.ts` и замените `RELEASE_URL`:

```typescript
export const RELEASE_CONFIG = {
  version: '3.0.0',
  
  // Вставьте сюда скопированный URL из GitHub Releases
  RELEASE_URL: 'https://github.com/ВАШ_USERNAME/avalon-checker-website/releases/download/v3.0.0/AvalonChecker-Setup-3.0.0.exe',
  
  // ... остальное без изменений
};
```

Сохраните и закоммитьте изменения:

```powershell
git add src/config/release.ts
git commit -m "Update release URL to GitHub Releases"
git push
```

---

## 🌐 Шаг 3: Настройка Vercel

### 3.1. Создайте аккаунт на Vercel (если нет)
- Перейдите на https://vercel.com/signup
- Войдите через GitHub

### 3.2. Импортируйте проект
1. На dashboard Vercel нажмите **"Add New Project"**
2. Выберите ваш репозиторий `avalon-checker-website`
3. Настройки проекта:
   - **Framework Preset:** Vite
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - **Install Command:** `npm ci`

### 3.3. Настройте переменные окружения (опционально)
Если нужны переменные окружения, добавьте их в разделе **"Environment Variables"**

### 3.4. Deploy
- Нажмите **"Deploy"**
- Дождитесь завершения (обычно 1-3 минуты)

---

## 🤖 Шаг 4: Автоматический деплой через GitHub Actions (опционально)

Если хотите автоматический деплой при каждом push:

### 4.1. Получите токены Vercel

**Vercel Token:**
1. Перейдите: https://vercel.com/account/tokens
2. Создайте новый токен → Скопируйте

**Vercel Org ID и Project ID:**
1. В папке проекта запустите:
   ```powershell
   npm i -g vercel
   vercel link
   ```
2. Следуйте инструкциям для подключения проекта
3. Найдите файл `.vercel/project.json`, там будут `orgId` и `projectId`

### 4.2. Добавьте secrets в GitHub

1. Перейдите в Settings вашего репозитория → Secrets and variables → Actions
2. Добавьте три секрета:
   - **VERCEL_TOKEN** - токен из шага 4.1
   - **VERCEL_ORG_ID** - orgId из .vercel/project.json
   - **VERCEL_PROJECT_ID** - projectId из .vercel/project.json

### 4.3. Готово!
Теперь при каждом push в `main` ветку сайт будет автоматически деплоиться на Vercel.

---

## 📦 Шаг 5: Как обновлять приложение в будущем

### Когда выходит новая версия приложения:

1. **Создайте новый GitHub Release:**
   - Tag: `v3.1.0` (новая версия)
   - Загрузите новый exe файл

2. **Обновите конфиг:**
   ```typescript
   // src/config/release.ts
   export const RELEASE_CONFIG = {
     version: '3.1.0',
     RELEASE_URL: 'https://github.com/.../v3.1.0/AvalonChecker-Setup-3.1.0.exe',
     sizeInMB: 520, // обновите размер если изменился
     // ...
   };
   ```

3. **Закоммитьте и запушьте:**
   ```powershell
   git add .
   git commit -m "Release v3.1.0"
   git push
   ```

4. Vercel автоматически задеплоит новую версию сайта!

---

## ✅ Проверка

После деплоя:
1. Откройте ваш сайт на Vercel (например, `https://avalon-checker.vercel.app`)
2. Перейдите на страницу **Download**
3. Нажмите кнопку скачивания
4. Убедитесь, что начинается загрузка exe файла из GitHub Releases

---

## 📊 Преимущества этого решения

✅ **Сайт легкий** - влезает в лимиты Vercel (<25 МБ)  
✅ **Файл доступен** - GitHub Releases бесплатен и быстр  
✅ **Автоматизация** - один push для обновления всего  
✅ **Версионность** - полная история всех релизов  
✅ **CDN** - GitHub использует CloudFlare для раздачи файлов  
✅ **Статистика** - можно видеть количество скачиваний  

---

## 🔧 Альтернативные решения (если GitHub не подходит)

### Вариант 2: Google Drive
1. Загрузите exe на Google Drive
2. Сделайте файл публичным
3. Получите прямую ссылку для скачивания
4. Используйте URL вида: `https://drive.google.com/uc?export=download&id=FILE_ID`

### Вариант 3: Cloudflare R2
1. Создайте bucket на Cloudflare R2 (10 GB бесплатно)
2. Загрузите файл
3. Сделайте публичным
4. Используйте публичный URL

### Вариант 4: AWS S3
1. Создайте S3 bucket
2. Настройте публичный доступ
3. Загрузите файл
4. Используйте S3 URL

---

## ❓ FAQ

**Q: Можно ли хостить exe файл прямо на Vercel?**  
A: Нет, Vercel не предназначен для больших статичных файлов. Используйте GitHub Releases или CDN.

**Q: Бесплатен ли GitHub Releases?**  
A: Да, полностью бесплатен для публичных и приватных репозиториев.

**Q: Есть ли лимит на размер файла в GitHub Releases?**  
A: Рекомендуется до 2 GB на файл, но для 600 MB это идеально подходит.

**Q: Будет ли работать скачивание из России?**  
A: GitHub может быть замедлен, рассмотрите альтернативы (Google Drive, Яндекс.Диск) для лучшей скорости.

**Q: Что если сайт занимает больше 25 МБ после билда?**  
A: Используйте code splitting, tree shaking, и оптимизацию изображений. Vite уже делает это автоматически.

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте build logs на Vercel
2. Убедитесь что все зависимости установлены
3. Проверьте что GitHub Release публичен
4. Попробуйте скачать файл напрямую по URL

---

**Готово! Ваш сайт на Vercel + exe файл на GitHub Releases = идеальное решение! 🎉**
