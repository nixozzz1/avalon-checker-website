# 🚀 Полная инструкция по деплою на Netlify с большим приложением

## Проблема и Решение

**Проблема:** Netlify имеет лимит на размер деплоя, но наш exe файл занимает ~600 МБ.

**Решение:** Использовать **GitHub Releases** для хостинга exe файла (бесплатно, без лимитов), а на Netlify размещать только легкий веб-сайт.

---

## 📋 Подготовка

### 1. Создайте GitHub репозиторий (если еще нет)

```powershell
cd d:\Test\CascadeProjects\2048\avalon-checker-website
git init
git add .
git commit -m "Initial commit: Avalon Checker Website"
```

Создайте новый репозиторий на GitHub: https://github.com/new

```powershell
git remote add origin https://github.com/ВАШ_USERNAME/avalon-checker-website.git
git branch -M main
git push -u origin main
```

---

## 🎯 Шаг 1: Создание GitHub Release с exe файлом

### 1.1. Перейдите на страницу Releases вашего репозитория
- `https://github.com/ВАШ_USERNAME/avalon-checker-website/releases`
- Нажмите **"Create a new release"**

### 1.2. Заполните информацию о релизе
- **Tag version:** `v3.0.0`
- **Release title:** `Avalon Checker v3.0.0`
- **Description:** Опишите изменения в версии

### 1.3. Загрузите exe файл
- Нажмите **"Attach binaries by dropping them here or selecting them"**
- Выберите файл: `d:\Test\CascadeProjects\2048\avalon-checker-website\public\release\AvalonChecker-Setup-3.0.0.exe`
- Дождитесь завершения загрузки (может занять несколько минут для 600 МБ)

### 1.4. Опубликуйте релиз
- Нажмите **"Publish release"**

### 1.5. Скопируйте URL загрузки
После публикации:
- Правой кнопкой на имя файла → **"Copy link address"**
- URL будет вида: `https://github.com/ВАШ_USERNAME/avalon-checker-website/releases/download/v3.0.0/AvalonChecker-Setup-3.0.0.exe`

---

## ⚙️ Шаг 2: Обновите конфигурацию релиза

Откройте файл `src/config/release.ts` и замените `RELEASE_URL`:

```typescript
export const RELEASE_CONFIG = {
  version: '3.0.0',
  
  // Вставьте сюда скопированный URL из GitHub Releases
  RELEASE_URL: 'https://github.com/ВАШ_USERNAME/avalon-checker-website/releases/download/v3.0.0/AvalonChecker-Setup-3.0.0.exe',
  
  // ... остальное без изменений
};
```

**ВАЖНО:** Удалите exe файл из проекта:

```powershell
Remove-Item "public\release\AvalonChecker-Setup-3.0.0.exe"
git add .
git commit -m "Use GitHub Releases for exe"
git push
```

---

## 🌐 Шаг 3: Настройка Netlify

### 3.1. Создайте аккаунт на Netlify (если нет)
- Перейдите на https://app.netlify.com/signup
- Войдите через GitHub

### 3.2. Импортируйте проект
1. На dashboard Netlify нажмите **"Add new site"** → **"Import an existing project"**
2. Выберите **"Deploy with GitHub"**
3. Авторизуйте Netlify для доступа к GitHub (если нужно)
4. Выберите ваш репозиторий `avalon-checker-website`

### 3.3. Настройки сборки
Netlify автоматически определит настройки из `netlify.toml`, но проверьте:

- **Branch to deploy:** `main`
- **Build command:** `npm run build`
- **Publish directory:** `dist`
- **Node version:** 20 (указано в netlify.toml)

### 3.4. Deploy
- Нажмите **"Deploy site"**
- Дождитесь завершения (обычно 2-4 минуты)

### 3.5. Получите URL сайта
После успешного деплоя вы увидите URL типа:
```
https://random-name-123456.netlify.app
```

Можно изменить имя:
- **Site settings** → **Change site name**

---

## 🔄 Автоматический деплой через GitHub (опционально)

Netlify автоматически отслеживает ваш GitHub репозиторий!

**При каждом push в main ветку:**
1. Netlify автоматически заметит изменения
2. Запустит новый build
3. Задеплоит обновлённую версию

Настроено автоматически - ничего дополнительно делать не нужно!

---

## 📦 Шаг 4: Как обновлять приложение в будущем

### Когда выходит новая версия приложения:

1. **Создайте новый GitHub Release:**
   - Tag: `v3.1.0` (новая версия)
   - Загрузите новый exe файл

2. **Обновите конфиг:**
   ```typescript
   // src/config/release.ts
   export const RELEASE_CONFIG = {
     version: '3.1.0',
     RELEASE_URL: 'https://github.com/.../v3.1.0/AvalonChecker-Setup-3.1.0.exe',
     sizeInMB: 520, // обновите размер если изменился
     // ...
   };
   ```

3. **Закоммитьте и запушьте:**
   ```powershell
   git add .
   git commit -m "Release v3.1.0"
   git push
   ```

4. Netlify автоматически задеплоит новую версию сайта!

---

## ✅ Проверка

После деплоя:
1. Откройте ваш сайт на Netlify (например, `https://avalon-checker.netlify.app`)
2. Перейдите на страницу **Download**
3. Нажмите кнопку скачивания
4. Убедитесь, что начинается загрузка exe файла из GitHub Releases

---

## 🎨 Настройка домена (опционально)

### Если хотите свой домен:

1. **В Netlify Dashboard:**
   - Откройте ваш сайт
   - **Domain settings** → **Add custom domain**
   - Введите ваш домен
   - Следуйте инструкциям по настройке DNS

2. **Netlify автоматически настроит:**
   - ✅ HTTPS сертификат (Let's Encrypt)
   - ✅ CDN для быстрой загрузки
   - ✅ Редирект с www на без www (или наоборот)

---

## 📊 Преимущества Netlify

✅ **Сайт легкий** - влезает в лимиты (<100 МБ)  
✅ **Файл доступен** - GitHub Releases бесплатен и быстр  
✅ **Автоматизация** - один push для обновления всего  
✅ **Версионность** - полная история всех релизов  
✅ **CDN** - Глобальная сеть доставки контента  
✅ **HTTPS** - Автоматический бесплатный SSL  
✅ **Preview** - Превью для каждого PR  

---

## 🔧 Особенности Netlify

### Что уже настроено в netlify.toml:

1. **Security Headers** - защита от XSS, clickjacking
2. **Cache Control** - оптимизация загрузки
3. **Redirects** - правильная маршрутизация SPA
4. **Environment** - Node.js 20

### Build настройки:
```toml
[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
```

---

## 📈 Мониторинг в Netlify

### Netlify Dashboard показывает:

- 📊 **Analytics** - посетители, страницы, источники
- 🚀 **Build history** - все деплои и их статусы
- 📝 **Deploy logs** - детальные логи сборки
- 🌍 **Bandwidth** - использованный трафик
- ⚡ **Performance** - скорость загрузки

---

## 🔄 Откат к предыдущей версии

Если что-то пошло не так:

1. **Netlify Dashboard** → **Deploys**
2. Найдите нужный предыдущий деплой
3. Нажмите **"Publish deploy"**
4. Готово - старая версия восстановлена!

---

## ❓ FAQ

**Q: Можно ли хостить exe файл прямо на Netlify?**  
A: Нет, Netlify не предназначен для больших статичных файлов. Используйте GitHub Releases.

**Q: Бесплатен ли Netlify?**  
A: Да, бесплатный план включает:
- 100 GB bandwidth/месяц
- 300 минут build time/месяц
- Автоматический HTTPS
- CDN

**Q: Есть ли лимит на размер сайта?**  
A: Рекомендуется до 100 МБ, но для Vite приложения это не проблема.

**Q: Что лучше - Netlify или Vercel?**  
A: Оба отличные! Netlify чуть проще в настройке, Vercel чуть быстрее. Для вашего случая оба подходят идеально.

**Q: Будет ли работать скачивание из России?**  
A: GitHub может быть замедлен, рассмотрите альтернативы (Яндекс.Диск, VK Документы) для лучшей скорости в РФ.

---

## 🆘 Решение проблем

### Build failed:
1. Проверьте **Deploy log** в Netlify
2. Убедитесь что `package.json` корректен
3. Проверьте что Node version = 20

### 404 при переходе по прямым ссылкам:
- Это нормально для SPA
- Уже настроено в `netlify.toml` (redirects)

### Exe файл не скачивается:
1. Проверьте GitHub Release - публичен ли он
2. Проверьте URL в `src/config/release.ts`
3. Попробуйте открыть URL напрямую в браузере

---

## 🎯 Итог

### Что у вас теперь есть:

1. ✅ **Легкий сайт на Netlify** (~0.5 МБ)
2. ✅ **Exe на GitHub Releases** (~600 МБ)
3. ✅ **Автоматические деплои** при push
4. ✅ **Бесплатный HTTPS** и CDN
5. ✅ **Deploy previews** для каждого PR

### Workflow обновления:

```
1. Новый exe → GitHub Release
2. Обновить src/config/release.ts
3. git push
4. Netlify автоматически обновит сайт
```

**Простой и эффективный процесс! 🚀**

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте **Deploy logs** на Netlify
2. Убедитесь что GitHub Release публичен
3. Попробуйте **Clear cache and deploy site**

---

**Готово! Ваш сайт на Netlify + exe на GitHub Releases = идеальное решение! 🎉**
